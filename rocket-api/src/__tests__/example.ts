import { addProducts } from '../resources/product/product.controller';

test('testing', () => {
    expect(addProducts).toBeDefined();
});

test('it works', () => {
    expect(addProducts).toBeDefined();
});
